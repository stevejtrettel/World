

//what is a map:
//it has an object with a shader material
//it has a texture that loads as a uniform
//it has an orientation that the sphere is in (default = north pole up)
class Map{
    constructor(){


        let mat;
    }
    //some methods to help build things up:
}